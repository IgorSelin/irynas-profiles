# 🧪 Як перевірити іконки локально

Цей документ описує різні способи перевірки, чи іконки правильно налаштовані та будуть відображатися в пошукових системах.

## 🚀 Швидка перевірка

### 1. Запустіть dev сервер

```bash
npm run dev
```

### 2. Відкрийте браузер

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

### 3. Перевірте вкладку браузера

- **У вкладці браузера** має з'явитися іконка з літерою "Л"
- Якщо іконки немає, перезавантажте сторінку (Ctrl+R або Cmd+R)

### 4. Перевірте через DevTools

1. Відкрийте **DevTools** (F12 або Cmd+Option+I)
2. Перейдіть на вкладку **Network**
3. Перезавантажте сторінку (Ctrl+R)
4. Знайдіть запити до:
   - `icon` - має повернути статус 200 (PNG 512x512)
   - `icon.svg` - має повернути статус 200
   - `apple-icon` - має повернути статус 200 (PNG 180x180)

### 5. Перевірте HTML код

1. Відкрийте **DevTools** (F12)
2. Перейдіть на вкладку **Elements** (або **Inspector**)
3. Знайдіть секцію `<head>`
4. Перевірте наявність тегів:
   ```html
   <link rel="icon" href="/icon" sizes="any" />
   <link rel="icon" href="/icon.svg" type="image/svg+xml" />
   <link rel="apple-touch-icon" href="/apple-icon" sizes="180x180" />
   ```

## 🧪 Автоматична перевірка

### Варіант 1: Використання скрипта

```bash
# Запустіть dev сервер в одному терміналі
npm run dev

# В іншому терміналі запустіть скрипт перевірки
./scripts/check-icons.sh
```

### Варіант 2: Використання Playwright тестів

```bash
# Запустіть тести (dev сервер запуститься автоматично)
npm test -- tests/icons.spec.ts
```

Або запустіть тільки тести іконок:

```bash
npx playwright test tests/icons.spec.ts
```

## 🌐 Перевірка через інструменти

### 1. Google Rich Results Test

**Важливо:** Цей інструмент працює тільки з публічними URL, не з localhost.

Після деплою:
1. Відкрийте [Google Rich Results Test](https://search.google.com/test/rich-results)
2. Введіть URL вашого сайту
3. Перевірте, чи іконка відображається правильно

### 2. Facebook Sharing Debugger

Після деплою:
1. Відкрийте [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
2. Введіть URL вашого сайту
3. Перевірте Open Graph зображення

### 3. Twitter Card Validator

Після деплою:
1. Відкрийте [Twitter Card Validator](https://cards-dev.twitter.com/validator)
2. Введіть URL вашого сайту
3. Перевірте Twitter Card зображення

## 🔍 Перевірка через curl

```bash
# Перевірка icon (PNG 512x512)
curl -I http://localhost:3000/icon

# Перевірка icon.svg
curl -I http://localhost:3000/icon.svg

# Перевірка apple-icon (PNG 180x180)
curl -I http://localhost:3000/apple-icon
```

Всі запити мають повернути статус `200 OK` та `Content-Type: image/png` (для `/icon` та `/apple-icon`) або `image/svg+xml` (для `/icon.svg`)

## 📋 Чеклист перевірки

- [ ] Іконка відображається у вкладці браузера
- [ ] `/icon` доступний (статус 200, PNG 512x512)
- [ ] `/icon.svg` доступний (статус 200)
- [ ] `/apple-icon` доступний (статус 200, PNG 180x180)
- [ ] HTML містить правильні `<link>` теги для іконок

## ⚠️ Важливі зауваження

1. **Google не індексує localhost** - для повної перевірки потрібен деплой на публічний сервер
2. **Кешування** - після змін іконок може знадобитися:
   - Очистити кеш браузера (Ctrl+Shift+Delete)
   - Використати режим інкогніто
   - Додати `?v=2` до URL іконки для примусового оновлення
3. **Час індексації** - Google може потребувати кілька днів для оновлення іконки в результатах пошуку
4. **Примусова індексація** - після деплою використайте Google Search Console для примусового оновлення індексу

## 🐛 Вирішення проблем

### Іконка не відображається у вкладці

1. Перевірте, чи dev сервер запущений
2. Очистіть кеш браузера
3. Перевірте консоль браузера на наявність помилок
4. Перевірте, чи файли іконок генеруються (перевірте Network tab)

### Помилка 404 для іконок

1. Переконайтеся, що `app/icon.tsx` та `app/apple-icon.tsx` існують
2. Перезапустіть dev сервер
3. Перевірте, чи немає помилок при збірці (`npm run build`)

### Іконка не оновлюється

1. Очистіть кеш браузера
2. Використайте режим інкогніто
3. Додайте версію до URL: `/favicon.ico?v=2`
4. Перезапустіть dev сервер
